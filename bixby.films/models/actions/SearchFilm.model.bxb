action (SearchFilm) {
  description (Search a film in the database)
  type (Search)
  collect {
    input (searchTerm) {
      type (SearchTerm)
      min (Required) max (One)
      // default-init {
      //   intent {
      //     goal: SearchTerm
      //   }
      // }
    }
    input (quest) {
      type (Quest)
      min (Optional) max (Many)
    }
  }
  output (Film)
}
