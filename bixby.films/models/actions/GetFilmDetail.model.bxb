action (GetFilmDetail) {
  description (Calls for film details and retrieves data)
  type (Fetch)
  collect {
    input (film) {
      type (Film)
      min (Required) max (One)
    }
  }
  output (FilmInfo)
}
