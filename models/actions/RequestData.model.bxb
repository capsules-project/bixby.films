action (RequestData) {
  description (User request a particular data)
  type (Constructor)
  collect {
    input (searchTerm) {
      type (SearchTerm)
      min (Required) max (Many)
    }
    input (quest) {
      type (Quest)
      min (Required) max (One)
    }
  }
  output (Film)
}
