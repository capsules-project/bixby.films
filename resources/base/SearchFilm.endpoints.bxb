endpoints {
  action-endpoints {
    action-endpoint (SearchFilm) {
      accepted-inputs (searchTerm, quest, $vivContext)
      local-endpoint (SearchFilm.js)
    }
    action-endpoint (GetFilmDetail) {
      accepted-inputs (film, $vivContext)
      local-endpoint (GetFilmDetail.js)
    }
  }
}
